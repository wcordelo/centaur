[project]
name = "tokenomist"
description = "Tokenomist — token unlock schedules, vesting, emissions, and allocations"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27.0",
    "typer>=0.12.0",
    "rich>=13.0.0",
    "python-dotenv>=1.0.0",
]

[project.scripts]
tokenomist = "centaur_tool_tokenomist.cli:app"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]

[tool.hatch.build.targets.wheel.sources]
"." = "centaur_tool_tokenomist"

[tool.centaur]
module = "client.py"
secrets = [{type = "http", name = "TOKENOMIST_API_KEY", mode = "inject", inject_header = "x-api-key", hosts = ["api.tokenomist.ai"]}]
