# eve Agent App

This project uses the eve framework. Before writing code, read the relevant guide
in `node_modules/eve/docs/` when the dependency is installed. If it is absent,
use the checked-in package metadata, implementation, and tests, and report the
missing local documentation as a limitation rather than blocking unrelated work.
