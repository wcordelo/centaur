# Centaur pins LiteLLM for in-cluster Codex /v1/responses (Gemini tool turns).
# Version is tracked in contrib/litellm/VERSION (see: just update-litellm-version).
ARG LITELLM_VERSION=v1.90.0
FROM ghcr.io/berriai/litellm:${LITELLM_VERSION}
