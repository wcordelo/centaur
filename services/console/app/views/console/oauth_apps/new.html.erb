<% content_for :title, "New OAuth app · Centaur Console" %>

<div class="mb-8">
  <a href="<%= console_oauth_apps_path %>" class="back-link">← back to OAuth apps</a>
  <div class="mt-2 flex flex-wrap items-center gap-3">
    <h1 class="page-title">New OAuth app</h1>
    <span class="resource-badge">OAuth</span>
  </div>
</div>

<%= render "errors", app: @app %>
<%= render "form", app: @app %>
