# Credential field sources and rules live in secret_sources.yml / request_rules.yml.
acme_gmail_oauth:
  namespace: acme
  foreign_id: oauth-gmail
  name: gmail
  labels:
    team: comms
  grant: refresh_token
  token_endpoint: https://oauth2.googleapis.com/token
  scopes:
    - https://www.googleapis.com/auth/gmail.readonly
  created_by: acme_admin
