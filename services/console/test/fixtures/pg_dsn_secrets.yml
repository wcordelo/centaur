acme_analytics_pg:
  namespace: acme
  foreign_id: pg-analytics
  name: analytics
  labels:
    team: data
  database: analytics
  role: readonly
  created_by: acme_admin

# No role set; exercises the optional-role path. Its DSN source lives in
# secret_sources.yml (pg_reporting_dsn).
acme_reporting_pg:
  namespace: acme
  foreign_id: pg-reporting
  name: reporting
  labels: {}
  database: reporting
  created_by: acme_admin
