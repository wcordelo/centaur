# Credential sources live in secret_sources.yml; the rule in request_rules.yml.
acme_cloudwatch_aws:
  namespace: acme
  foreign_id: aws-cloudwatch
  name: cloudwatch-reader
  labels:
    team: platform
  allowed_regions: []
  allowed_services:
    - logs
    - monitoring
  created_by: acme_admin
